---
- name: Setup official Docker repository
  import_tasks: repository.yml
  tags:
    - docker
    - docker-repo

- name: Install Docker Engine packages
  import_tasks: install.yml
  tags:
    - docker
    - docker-install

- name: Configure Docker daemon
  import_tasks: daemon.yml
  tags:
    - docker
    - docker-daemon

- name: Initialize platform network layer
  import_tasks: network.yml
  tags:
    - docker
    - docker-network

- name: Perform automated health verification
  import_tasks: verify.yml
  tags:
    - docker
    - docker-verify
